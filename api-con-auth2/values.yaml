replicaCount: 1

image:
  repository: api-con-auth
  tag: "1.3" 

service:
  type: NodePort
  port: 5000
  targetPort: 5000
  nodePort: 30001


postgres: 
  secretName: postgres-secret
  storage: 1Gi

# Application port configuration
app:
  port: 5000
